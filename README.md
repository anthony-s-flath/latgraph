# Latgraph

A small Python library for graphing lattices and related concepts with Plotly.

## Requirements

* NumPy
* Plotly
* Kaleido (for SVG export)

## Layout

* `src/graph.py` — lattice-specific graphing functions
* `src/figure.py` — Plotly helpers and styling
* `main.py` — examples

## Customization

Plot styling is configured in `src/figure.py`.

